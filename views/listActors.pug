extends master.pug
block title
    | Actor List
block content
    h1= 'List of Actors'
    table
    ul(class='verticalList')
        for actor in actorList
            li
                a(href='actor/'+actor.actorID) #{actor.name}, born on #{actor.birthday}
