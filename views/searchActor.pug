extends master.pug
block title
    | Search for an Actor
block content
    h2 Search for an Actor
    form.narrow(method='POST' action='/searchActor')
        input#email.form-control(type='text' name='actorName' placeholder='Actor Name' autocomplete='off' required)
        button.btn.btn-primary(type='submit') Search
